import 'package:flutter/material.dart';

class CustomeAppLogoWidget extends StatelessWidget {
  const CustomeAppLogoWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const CircleAvatar();
  }
}
